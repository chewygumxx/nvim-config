# vim:set expandtab shiftwidth=4 filetype=yaml:
# SPDX-License-Identifier: GPL-3.0-only

#
#
# ~chewygumxx/nvim-config.git
# ::: :/luajit.yml
#
#

# selene standard library for LuaJIT's extensions over vanilla Lua 5.1,
# which Neovim runs on. Referenced from `selene.toml`'s `std`.
---
globals:
    jit:
        any: true
    bit:
        any: true
    ffi:
        any: true
